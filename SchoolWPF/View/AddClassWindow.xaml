<Window x:Class="SchoolWPF.View.AddClassWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchoolWPF.View"
        mc:Ignorable="d"
        Title="AddClassWindow" Height="200" Width="350">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Название класса (например, 4б)" Margin="0,0,0,5"/>
            <TextBox Text="{Binding NewClassName, UpdateSourceTrigger=PropertyChanged}" Height="32"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="90" Height="30" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="90" Height="30"
                Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
