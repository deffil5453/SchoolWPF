<UserControl x:Class="SchoolWPF.View.UserControls.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchoolWPF.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="👨‍🎓 Список учеников" Style="{StaticResource PageTitle}"/>

        <!-- Кнопки управления -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Добавить ученика" 
                        Command="{Binding AddStudentCommand}" 
                        Style="{StaticResource SuccessButton}"
                        Width="180" Margin="0,0,10,0"/>
                <Button Content="🗑 Удалить выбранного" 
                        Command="{Binding DeleteStudentCommand}" 
                        Style="{StaticResource DangerButton}"
                        Width="170"/>
            </StackPanel>
        </Border>

        <!-- Таблица учеников -->
        <DataGrid Grid.Row="2" Style="{StaticResource ModernDataGrid}"
                  ItemsSource="{Binding Students}" 
                  SelectedItem="{Binding SelectedStudent}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="1.3*" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="1.3*"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="1.3*"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" Width="1*"/>
                <DataGridTextColumn Header="Класс" Binding="{Binding Class.ClassName}" Width="0.9*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>