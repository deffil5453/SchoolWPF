<UserControl x:Class="SchoolWPF.View.UserControls.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchoolWPF.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="📅 Расписание уроков" Style="{StaticResource PageTitle}"/>

        <!-- Панель выбора класса -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Выберите класс:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox ItemsSource="{Binding Classes}"
                          DisplayMemberPath="ClassName"
                          SelectedItem="{Binding SelectedClass}"
                          Style="{StaticResource ModernComboBox}"
                          Width="140" Margin="0,0,20,0"/>
                <Button Content="📥 Загрузить расписание"
                        Command="{Binding LoadScheduleCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Width="180"/>
            </StackPanel>
        </Border>

        <!-- Таблица расписания -->
        <DataGrid Grid.Row="2" Style="{StaticResource ModernDataGrid}"
                  ItemsSource="{Binding ScheduleRows}"
                  SelectedItem="{Binding SelectedScheduleRow}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Урок" Binding="{Binding LessonNumber}" Width="0.7*" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Понедельник" Binding="{Binding Monday}" Width="*"/>
                <DataGridTextColumn Header="Вторник" Binding="{Binding Tuesday}" Width="*"/>
                <DataGridTextColumn Header="Среда" Binding="{Binding Wednesday}" Width="*"/>
                <DataGridTextColumn Header="Четверг" Binding="{Binding Thursday}" Width="*"/>
                <DataGridTextColumn Header="Пятница" Binding="{Binding Friday}" Width="*"/>
                <DataGridTextColumn Header="Суббота" Binding="{Binding Saturday}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
